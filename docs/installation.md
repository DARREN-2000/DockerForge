# Installation

DockerForge requires Python 3.10 or higher.

## Installing from Source

Currently, DockerForge is available by cloning the repository:

```bash
git clone https://github.com/dockerforge-org/dockerforge.git
cd dockerforge
python -m pip install -e .
```

Verify the installation:

```bash
dockerforge --help
```
